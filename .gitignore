# Terraform state files — never commit these
*.tfstate
*.tfstate.backup
*.tfstate.lock.info
.terraform/
.terraform.lock.hcl
terraform.tfvars

# AWS credentials — never commit
.aws/
credentials

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
